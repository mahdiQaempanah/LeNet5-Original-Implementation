# config.yaml
model:
  A: 1.7159
  S: 0.6666666666

paths:
  rbf_file_format: "static/RBF_kernel/{id}_RBF.jpg"
  dataset_dir: "static/dataset/"
  run_summary_dir: "runs/experiment_1/"

training:
  lr: 0.005
  training_size: 20000
  batch_size: 1024
  num_epochs: 30
  shuffle: true
  num_workers: 10